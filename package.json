{
  "name": "steam-web-extensions",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build-serve": "npx http-server --cors -c-1 dist",
    "preview": "vite preview"
  },
  "dependencies": {
    "preact": "^10.5.15",
    "sass": "^1.50.1"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.1.5",
    "typescript": "^4.5.4",
    "vite": "^2.9.5"
  }
}
